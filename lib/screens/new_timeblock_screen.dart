import 'package:flutter/material.dart';

class NewTimeblockScreen extends StatelessWidget {
  const NewTimeblockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New timeblock'),
      ),
      body: Center(
        child: Text('E tal'),
      ),
    );
  }
}
